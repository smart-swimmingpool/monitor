# MegaLinter Configuration for Monitor Module
# Replaces Super-Linter with equivalent rules
# https://megalinter.io/latest/configuration/

---
# Enable only the linters that were enabled in Super-Linter
# Note: CPP is disabled because most C++ files are external libraries (GxDEPG0213BN, GxGDE0213B72B)
# which have formatting issues that are not relevant for our code quality
ENABLE:
  - BASH
  - EDITORCONFIG
  - GITHUB_ACTIONS
  - GITLEAKS
  - JSON
  - MARKDOWN
  - YAML

# Disable specific linters
DISABLE:
  - CPP

# Filter patterns - exclude build artifacts and dependencies
# Must be a string, not a list
FILTER_REGEX_EXCLUDE: |
  .*/(\.pio|\.vscode|\.platformio|build|\.git|node_modules)/.*
  .*/lib/.*
  .*/data/.*
  .*\.min\.(js|css)
  .*/test/.*

# Markdown specific settings - line length 150
MARKDOWN_LINT_LINE_LENGTH: 150

# Use custom configuration files
MARKDOWNLINT_CONFIG_FILE: .markdownlint.yml
YAMLLINT_CONFIG_FILE: .yamllint.yml

# Don't treat warnings as errors
WARNINGS_AS_ERRORS: false

# Logging
LOG_LEVEL: NOTICE

# Parallel execution
PARALLEL: false
